export interface ThematicUnit {
    name: string;
    url: string;
    id: string;
    description: string;
    imageLink: string;
}

export interface PractiseLecuture {
    name: string;
    id: string;
    url: string;
}